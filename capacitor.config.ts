import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eatsmart.app',
  appName: 'eatsmart',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
